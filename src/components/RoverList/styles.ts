import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    roversList: {
        width: '50%',
    },
    roverPhoto: {
        position: 'absolute',
        zIndex: '-100',
        right: '3rem',
        top: '4rem',
        maxHeight: '40rem',
    },
});
